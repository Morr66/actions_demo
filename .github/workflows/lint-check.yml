name: Code Quality Check
on:
  pull_request:
    branches: [main]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - checkout
      - setup-python
      - install flake8
      - check code style